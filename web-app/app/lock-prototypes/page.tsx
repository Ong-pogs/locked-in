'use client';

import { PrototypesScene } from './PrototypesScene';

/**
 * /lock-prototypes — visual prototype switcher for the "Lock Your Funds"
 * onboarding deposit page. Mock data only, no API. Compares four UX
 * treatments side-by-side via a localStorage-persisted variant pill.
 */
export default function LockPrototypesPage() {
  return <PrototypesScene />;
}
