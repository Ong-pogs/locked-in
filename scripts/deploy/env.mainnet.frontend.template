# ── Mainnet frontend env (Vercel — build-time NEXT_PUBLIC_*) ─────────────────
# These bake into the client bundle at build. A git push / redeploy is required
# after changing them (vercel redeploy alone does NOT rebuild env).
# All values here are public (program id, mint, oracle) — no secrets.

NEXT_PUBLIC_SOLANA_CLUSTER=mainnet-beta
NEXT_PUBLIC_SOLANA_RPC_URL=https://<your-paid-mainnet-rpc>

# v2 vault custody
NEXT_PUBLIC_VAULT_V2_PROGRAM_ID=FAuFtXbTAT9SiJTghxdZ1ZD4ShgrdTk2EqgyPxfq2gZ6
NEXT_PUBLIC_LOCK_VAULT_USDC_MINT=EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v

# Kamino refresh_reserve scope oracle for the USDC reserve (defaults to this if
# unset; set explicitly for clarity).
NEXT_PUBLIC_KAMINO_SCOPE_PRICES=3t4JZcueEzTbVP6kLxXrL3VpWx45jDer4eqysweBchNH
