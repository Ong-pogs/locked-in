[package]
name = "locked-in"
version = "0.1.0"
description = "Merged custody (vault) + community-pot program for Locked In v4"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "locked_in"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = { version = "0.31.1", features = ["init-if-needed"] }
anchor-spl = { version = "0.31.1", features = ["associated_token"] }
